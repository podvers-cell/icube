"use client";

import CustomCursor from "./CustomCursor";

/** Renders the custom cursor. Default cursor is hidden globally via globals.css. */
export default function CustomCursorProvider() {
  return <CustomCursor />;
}
